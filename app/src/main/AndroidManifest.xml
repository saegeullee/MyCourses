<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saegeullee.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!-- <uses-feature -->
    <!-- android:glEsVersion="0x00020000" -->
    <!-- android:required="true"/> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavigationActivity.SearchActivity" />
        <activity android:name=".NavigationActivity.WishListActivity" />
        <activity android:name=".ChatActivity.ChatActivity" />
        <activity android:name=".MoreActivity.MoreActivity" />
        <activity android:name=".NavigationActivity.MainActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".MoreActivity.AdminActivity" />
        <activity android:name=".MoreActivity.AddCourseActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".MoreActivity.AccountSettingActivity" />
        <activity android:name=".CourseDetailsActivity" />
        <activity android:name=".MoreActivity.TutorAccountCheckActivity" />
        <activity android:name=".TutorCourseCheckActivity" />
        <activity android:name=".CreateAccountNextActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".EnrollCourseActivity" />
        <activity android:name=".MoreActivity.AppliedCourseCheckActivity" />
        <activity android:name=".MoreActivity.OpenedCourseActivity" />
        <activity android:name=".MoreActivity.EnrolledCourseActivity" />
        <activity android:name=".CourseOpenedDetailsActivity" />
        <activity android:name=".EnrolledStudentsCheckActivity" />
        <activity android:name=".EnrolledStudentsAccountCheckActivity" />
        <activity android:name=".ChatActivity.ChatRoomActivity" />
        <activity android:name=".MoreActivity.NoticeActivity" />
        <activity android:name=".MoreActivity.NoticeContentActivity" />
        <activity android:name=".TutorDetailsActivity"></activity>
    </application>

</manifest>